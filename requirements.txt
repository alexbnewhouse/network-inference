numpy>=1.26
pandas>=2.2
networkx>=3.2
tqdm>=4.66
matplotlib>=3.8
seaborn>=0.13
pyvis>=0.3.2
scipy>=1.12
polars>=0.20
python-igraph>=0.11
spacy[transformers]>=3.7
python-louvain>=0.16
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
vaderSentiment>=3.3.2  # required for VADER sentiment analysis
textblob>=0.17.1  # alternative sentiment analyzer
bertopic>=0.16  # optional: required for topic modeling notebook
cupy-cuda12x; platform_system=="Linux" or platform_system=="Windows"

# Optional: GPU-Accelerated Transformer Sentiment Analysis
# Uncomment if you want to use transformer-based sentiment with GPU support
# torch>=2.0.0  # PyTorch with CUDA for NVIDIA GPUs
# transformers>=4.30.0  # HuggingFace Transformers
# accelerate>=0.20.0  # GPU acceleration utilities
#
# Installation instructions:
# For NVIDIA GPU: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# For Apple Silicon: pip install torch torchvision torchaudio
# Then: pip install transformers accelerate

# Optional: FAISS for efficient similarity search 
# ⚠️ NOT RECOMMENDED for Python 3.12+ due to compatibility issues (segfaults)
# The default batch processing method is stable and production-ready
#
# If you're on Python 3.9-3.11 and want to try FAISS:
# faiss-cpu>=1.7.4  # CPU version
# faiss-gpu>=1.7.4  # GPU version (NVIDIA only)
#
# Installation instructions (Python 3.9-3.11 only):
# For CPU: pip install faiss-cpu
# For GPU: pip install faiss-gpu
#
# See GPU_SENTIMENT_GUIDE.md for detailed setup instructions